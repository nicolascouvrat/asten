#ifndef GUARD_STREAMS_H
#define GUARD_STREAMS_H

namespace utils {

// StreamMode are the different modes in which utils::XXStream can be used.
enum StreamMode {
  IN,
  OUT,
};

} // namespace utils

#endif
